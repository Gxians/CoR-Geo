report_split: val
main_fovs: [360, 180, 90, 70]
auxiliary_fovs: []

exact:
  encode_dtype: float32
  descriptor_storage_dtype: float32
  score_dtype: float32
  allow_tf32: false

metrics:
  recall_ks: [1, 5, 10]
  # The unqualified R@1% uses floor(N/100); the evaluator also records ceil.
  r1_percent_rounding: floor
